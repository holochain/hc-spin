name: Publish Release

on:
  push:
    branches: [main]

jobs:
  publish:
    permissions:
      contents: write
      id-token: write
    uses: holochain/actions/.github/workflows/nodejs-publish-release.yml@fix-nodejs-publish
    with:
      package_manager: yarn
